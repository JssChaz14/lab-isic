import { ReserveLaboratory } from '../../src/app/models/reserve-laboratory';

describe('ReserveLaboratory', () => {
  it('should create an instance', () => {
    expect(new ReserveLaboratory()).toBeTruthy();
  });
});
